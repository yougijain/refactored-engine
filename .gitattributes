# The pipeline writes LF everywhere so CI can byte-compare regenerated output
# against what is committed. Do not let git rewrite line endings.
* text=auto eol=lf
*.csv text eol=lf
*.twb text eol=lf

# Tableau schemas are vendored verbatim; keep their bytes identical to upstream.
tableau/schema/twb_*.xsd -text
